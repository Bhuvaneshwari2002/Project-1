# Project-1
📌**CLIENT QUERY MANAGEMENT SYSTEM**

📖Overview:
Client Query Management System is a simple web application built using Streamlit where:

→clients can login & raise their queries 

→support team members can login and view all queries,close open queries and can see the history of closed queries


🚀Features:

🔓Authentication:-

→Role-based login (Client / Support).

→Passwords are securely stored as hashed values (SHA256).

👤Client Dashboard:-

→Submit new queries with email, mobile number, heading, and description.

→Queries are automatically marked as Opened with timestamp.

🛠Support Dashboard:-

→View all queries (Open & Closed).

→Filter queries by status (All / Opened / Closed).

→Close open queries.

→View full query history.

🔒Logout:-

→Logout button for switching between roles.

🛠 Tech Stack:

→Frontend / App: Python, Streamlit

→Database: MySQL

📚Libraries:-

→pymysql

→hashlib

→pandas, numpy


→Logout button for switching between roles.

